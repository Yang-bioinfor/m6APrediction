% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_single}
\alias{prediction_single}
\title{Predict m6A status for a single sample}
\usage{
prediction_single(
  ml_fit,
  gc_content,
  RNA_type,
  RNA_region,
  exon_length,
  distance_to_junction,
  evolutionary_conservation,
  DNA_5mer,
  positive_threshold = 0.5
)
}
\arguments{
\item{ml_fit}{Fitted \code{randomForest} model (same as in \code{prediction_multiple()}).}

\item{gc_content}{Numeric GC content.}

\item{RNA_type}{Character; one of \code{"mRNA"}, \code{"lincRNA"}, \code{"lncRNA"},
\code{"pseudogene"}.}

\item{RNA_region}{Character; one of \code{"CDS"}, \code{"intron"}, \code{"3'UTR"},
\code{"5'UTR"}.}

\item{exon_length}{Numeric length of the exon.}

\item{distance_to_junction}{Numeric distance to nearest splice junction.}

\item{evolutionary_conservation}{Numeric conservation score.}

\item{DNA_5mer}{Character 5-mer DNA sequence (e.g., \code{"ATGCC"}).}

\item{positive_threshold}{Numeric threshold (default 0.5).}
}
\value{
Named vector of length 2:
\code{predicted_m6A_prob} (numeric) and \code{predicted_m6A_status}
(character \code{"Positive"} or \code{"Negative"}).
}
\description{
Wrapper around \code{prediction_multiple()} that accepts individual feature
values instead of a data frame. Returns a named vector with the predicted
probability and status.
}
\examples{
# Load example model included in the package
model <- readRDS(system.file("extdata", "rf_fit.rds",
                             package = "m6APrediction"))
prediction_single(
  ml_fit = model,
  gc_content = 0.45,
  RNA_type = "mRNA",
  RNA_region = "CDS",
  exon_length = 120,
  distance_to_junction = 30,
  evolutionary_conservation = 0.8,
  DNA_5mer = "ATGCC"
)

}
